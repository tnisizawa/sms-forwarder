# Changelog

このプロジェクトの版は `gas/lib.js` の `VERSION` 定数と突き合わせられます。
`settings` シートの「バージョン」行にも同じ番号が表示されます。

## 1.1.0

- 転送メールの件名を `[SMS転送]` に固定し、`In-Reply-To`/`References` と `threadId` で
  すべての転送メールを Gmail の1つのスレッドへまとめるようにした。
  以前の件名 `[SMS] <送信元番号>` では送信元ごとに別スレッドになり、番号が違うと
  まとまらなかった。送信元は引き続き本文とラベルで区別できる

## 1.0.0

配布可能な最初の版。

- Android 端末の SMS を GAS ウェブアプリ経由で Gmail へ転送（複数端末対応）
- `settings` シートによる設定管理（合言葉・転送先・ラベル・ログ保持・ウェブアプリURL）
- `filter` シートによる allow / deny フィルター、`log` シートへの全件記録と保持行数ローテーション
- スプレッドシートの「SMS転送」メニュー（初期設定 / 手順メール / テスト送信）
- 重複受信の排除（短時間の同一内容 POST を dedup）
- スマホ側は手順メールに埋め込んだ値を貼るだけ（Gmail のパスワードは端末に置かない）
